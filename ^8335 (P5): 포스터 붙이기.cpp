#include <iostream>
using namespace std;
int main() {
    int cnt; cin >> cnt;
    vector<pair<int, int>> b(cnt); //first, second = count, height
    for (auto& p : b) cin >> p.first >> p.second();
    
}

#include <iostream>
#include <vector>
using namespace std;
int main() {
	int cnt; cin >> cnt;
	vector<int> s(cnt);
	for (int i = 1, p; i <= cnt; i++) {
		cin >> p;
		s.insert(s.begin() + p, i);
	}
	while (cnt--) cout << s[cnt] << ' ';
}
